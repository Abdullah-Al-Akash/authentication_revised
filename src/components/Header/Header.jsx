import React from 'react';
import { Link } from 'react-router-dom';

const Header = () => {
    return (
        <div>
            <h1>This is Header!</h1>
        </div>
    );
};

export default Header;